cat > .env.example << 'EOF'
# Copy this file to .env and adjust as needed.

# Database (for Docker / remote)
POSTGRES_USER=cardiac_user
POSTGRES_PASSWORD=cardiac_password
POSTGRES_DB=cardiac_db
POSTGRES_HOST=db
POSTGRES_PORT=5432

# SQLAlchemy DB URL for backend (Docker)
DATABASE_URL=postgresql+psycopg2://cardiac_user:cardiac_password@db:5432/cardiac_db

# App
APP_ENV=cloud_dev
APP_NAME=IS Cardiac Agent Backend
APP_LOG_LEVEL=info
EOF
